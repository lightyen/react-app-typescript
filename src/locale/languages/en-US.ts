import { Locale } from "."

const locale: Locale = {
    locale: "en-US",
    pluralRuleFunction: () => "",
    fields: {
        text: "React is awesome!",
        hello: "Hello",
    },
}

export default locale
