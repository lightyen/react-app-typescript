<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
        <%= htmlWebpackPlugin.files.css.map(url => `
        <link rel="stylesheet" type="text/css" href="${url}" />
        `).join("") %>
        <style>
            html,
            body {
                height: 100%;
            }
            body {
                margin: 0;
            }
        </style>
    </head>
    <body>
        <div id="404"></div>
        <%= htmlWebpackPlugin.options.dll %> <%= htmlWebpackPlugin.files.js.map(url => `
        <script type="text/javascript" src="${url}"></script>
        `).join("") %>
    </body>
</html>
